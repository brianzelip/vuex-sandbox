export const ADD_ITEM_TO_USER_SELECTION = ({ commit }, payload) => {
  commit('ADD_ITEM_TO_USER_SELECTION', payload);
};

export const REMOVE_ITEM_FROM_USER_SELECTION = ({ commit }, payload) => {
  commit('REMOVE_ITEM_FROM_USER_SELECTION', payload);
};
